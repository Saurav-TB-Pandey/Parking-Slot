create database Parking_Slot;
use Parking_Slot ;
Create table user_details( First_Name varchar(20), Last_Name varchar(20), mobileNo bigint unique, Vehicle_Type varchar(20), Vehicle_No varchar(20));

select *from user_details;